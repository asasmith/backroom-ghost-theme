<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />
    <script src="{{asset "built/index.js"}}" defer></script>

    <title>{{meta_title}}</title>

    {{ghost_head}}
    {{!-- Outputs important meta data and settings, should always be in <head> --}}

</head>
<body class="{{body_class}} font-bodyFont text-xl">
<div class="gh-viewport">

<header id="gh-head" class="gh-head bg-black text-white px-2 lg:px-0">
    <nav class="gh-head-inner gh-container flex justify-between md:items-center py-4">

        <div class="gh-head-brand">
            <a class="gh-head-logo text-4xl flex flex-col font-headingFont font-thin" href="{{@site.url}}">
                {{#if @site.logo}}
                    <img src="{{@site.logo}}" alt="{{@site.title}}" />
                {{else}}
                    {{@site.title}}
                    <span class="text-base uppercase font-bodyFont">brand insiders</span>
                {{/if}}
            </a>
            <a class="gh-burger" role="button" aria-label="menu">
                <div class="gh-burger-box">
                    <div class="gh-burger-inner"></div>
                </div>
            </a>
        </div>
        <div class="gh-head-menu">
            {{navigation}}
        </div>
    </nav>
</header>

<main class="gh-main">

    {{{body}}}
    {{!-- All content gets inserted here, index.hbs, post.hbs, etc --}}

</main>

<footer class="gh-foot bg-black text-white py-10">
    <div class="gh-container flex justify-end">
        <div class="flex flex-col text-base">
            <a href="mailto:noreply@kararedman.com" class="mb-4 w-fit border-b border-black hover:border-white">Email Me</a>
            <a href="https://www.linkedin.com/in/kararedman/" target="_blank" class="border-b border-black hover:border-white">Follow me on Linkedin</a>
        </div>
    </div>
</footer>

</div>

{{!-- <foot> --}}
{{ghost_foot}}
{{!-- Outputs important scripts - should always be included before closing body tag --}}

</body>
</html>